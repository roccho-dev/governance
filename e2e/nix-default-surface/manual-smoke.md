# Manual smoke

Expected after checkout:

- run the default app and see help;
- run the named help app and see the same help;
- build the named bootstrap input package;
- run all flake checks;
- keep implicit package build unsupported unless a later accepted ADR allows it.
