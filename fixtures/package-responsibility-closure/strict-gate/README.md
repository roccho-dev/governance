# package closure strict gate fixture

This fixture note exists to mark the strict-gate PR as intentionally stacked on `proposal/package-responsibility-closure-compilers-260701`.

The executable fixture is generated by `tools/check-package-closure-strict.py selftest`:

- dirty package closure fixture in strict mode must fail;
- dirty package closure fixture in shadow mode must be `report-generated`, not pass;
- clean package closure fixture in strict mode must pass.
