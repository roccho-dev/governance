# required repo packet org join fixtures

These fixtures cover Phase D:

- D1 required repo universe reader
- D2 packet registry / transport / pinning
- D3 packet finding emission
- D4 provenance-aware org join

The fixture selftest is tool evidence only. It does not claim final gate cutover, branch protection, downstream rollout, or final all-package admission.

Run:

```text
python3 tools/check-package-required-repo-org-join.py selftest
```
