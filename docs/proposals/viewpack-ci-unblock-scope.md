# Viewpack CI unblock scope

This lane only carries the existing gov-lib contract implementation cleanup to the `proposals` base so Viewpack PRs are evaluated through declared `nix flake check` only.

It must not add Markdown rendering, artifact upload ownership, or repo mutation behavior.
