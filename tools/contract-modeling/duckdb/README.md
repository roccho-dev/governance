# DuckDB gate plane

`gates.sql` evaluates the V1 admission invariants. `abi.sql` exposes the stable
version-normalizing package contract ABI. DuckDB is a deterministic projection
and gate plane; it is not accepted meaning authority.
